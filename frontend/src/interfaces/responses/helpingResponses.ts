export interface BookAuthorResponse {
    id: string;
    fullName: string;
}

export interface EditorResponse {
    id: string;
    fullName: string;
}

export interface PublishingHouseResponse {
    id: string;
    name: string;
}

export interface PublishingPlaceResponse {
    id: string;
    name: string;
}